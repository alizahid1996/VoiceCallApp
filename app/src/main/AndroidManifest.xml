<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.voicecallapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VoiceCallApp">
        <activity android:name=".Registration.SignUpActivity"></activity>
        <activity
            android:name=".SplashActivity"
            android:theme="@style/Theme.VoiceCallApp.NoActionbar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginScreen.ResetPasswordActivity"
            android:theme="@style/Theme.VoiceCallApp.NoActionbar" />
        <activity
            android:name=".LoginScreen.OTPVerifyActivity"
            android:theme="@style/Theme.VoiceCallApp.NoActionbar" />
        <activity
            android:name=".LoginScreen.ForgotPasswordActivity"
            android:theme="@style/Theme.VoiceCallApp.NoActionbar" />
        <activity
            android:name=".LoginScreen.LoginActivity"
            android:theme="@style/Theme.VoiceCallApp.NoActionbar" />
        <activity android:name=".MainActivity"></activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage"
        tools:remove="android:maxSdkVersion" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

</manifest>